#'  @param object The object being validated. Note that the object must have a
#'  private member list called 'params', containing the names of the
#'  variables to be evaluated, their values, the appropriate valid
#'  values as well as other parameters used by the validation methods.
#'  A getParams method provides the validation methods access
#'  to this private member.
